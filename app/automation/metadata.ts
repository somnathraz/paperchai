import { generateMetadata } from "@/lib/seo-config";

export const metadata = generateMetadata({
  title: "Automation",
  description:
    "Set up automated workflows for invoicing, payment reminders, and client communications.",
  path: "/automation",
  noIndex: true,
});
